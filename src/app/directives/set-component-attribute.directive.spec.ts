import { SetComponentAttributeDirective } from './set-component-attribute.directive';

describe('SetComponentAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new SetComponentAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
